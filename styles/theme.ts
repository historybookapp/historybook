import { extendTheme } from '@chakra-ui/react'

const colors = {
  brand: {
    blue: '#14213D',
    orange: '#FCA311',
    platinum: '#E5E5E5',
  },
}
const theme = extendTheme({ colors })

export default theme
